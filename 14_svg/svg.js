// Get the body
var body = d3.select("body");

// Get the chart div
var chart = d3.selectAll("div");

// Get the bar divs + chart div 
var bars = d3.selectAll("div");

bars.append("span").text("A PPENDING....")
